# AI module contract

The `frontend/src/ai` implementation is the live inference path in V1. The backend `app/ai` directory is reserved for server-side/offline inference services.

Do not pretend the placeholder backend `/api/v1/ai/analyze` is a trained AI model. A future model adapter should implement a stable interface and report its model name/version and confidence.
