# Used only for local `npm run dev`. Leave unset (or copy this file to .env)
# to talk directly to a locally-running backend on port 8000.
# In Docker, this is intentionally overridden to "" at build time (see
# docker-compose.yml) so the browser calls the app through the Nginx proxy.
VITE_API_BASE=http://localhost:8000
